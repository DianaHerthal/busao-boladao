char *randstring(size_t length); //retorna (length) caracteres

char *NameGen(); //retorna nome gerado aleatoriamente

int compara_nome(const void *a, const void *b); //comparador pra ser usado na funcao ordena

int compara_cpf(const void *a, const void *b); //comparador pra ser usado na funcao ordena